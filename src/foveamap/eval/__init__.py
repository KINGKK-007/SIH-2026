"""foveamap.eval — Evaluation metrics and experiment runners.

Phase 6 implementation (stub).

Planned modules
---------------
metrics  : mIoU, per-class IoU, instance recall/precision.
ranges   : Range-bucket assignment and per-bucket aggregation.
latency  : Per-stage timing protocol (warm-up, p50/p95/p99).
memory   : Cell count and byte accounting for all four baselines.
"""
