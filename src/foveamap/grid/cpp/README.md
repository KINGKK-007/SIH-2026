# Optional C++ grid backend (tier P2)

pybind11 sources for `foveamap._fovea_cpp` live here. Built only when triggered by Phase 7, task T7.5
(NumPy grid-stage p95 > 15 ms on 100 frames, or time remains after P0/P1). Must be bit-identical to the
NumPy reference backend (invariant I8).
