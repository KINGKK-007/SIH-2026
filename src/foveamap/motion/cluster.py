"""Range-scaled Euclidean clustering of movable points (README 6.4 step 4, task T10.2)."""

from __future__ import annotations

import numpy as np


def cluster_points(xyz: np.ndarray, cfg: object) -> np.ndarray:
    """Return a cluster id per point (-1 = noise)."""
    raise NotImplementedError("Implemented in Phase 10, T10.2 (docs/PHASES.md).")
